extends base 
block content
        h2 Profile Page
        .user-container
            h2
                p Name : #{user.name}
                p Email : #{user.email}
                p
                    a(href="/") Settings
                if user.role == "admin"
                    p
                        a(href="/managePlans") Manage Plans
                    p
                        a(href="/manageUser") Manage User


